{
  "name": "openmpsync-worker",
  "version": "0.1.0",
  "private": true,
  "description": "OpenMPSync sync server - Cloudflare Worker + R2 edition (GPL-3.0)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
